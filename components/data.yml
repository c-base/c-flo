component: ingress-table/data
label: Ingress data fetching for the Ingress Table
icon: fire
inports:
  fetch:
    type: all
    queue: ingress-data.FETCH
  poll:
    type: all
    queue: ingress-data.POLL
outports:
  floor:
    type: all
    queue: ingress-data.FLOOR
  light:
    type: all
    queue: ingress-data.LIGHT
  streets:
    type: all
    queue: ingress-data.STREETS
